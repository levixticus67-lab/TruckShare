[build]
  base = "artifacts/returnhaul"
  command = "pnpm --dir ../.. install --frozen-lockfile && pnpm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "22"
  PORT = "5173"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200